<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title || 'Novo Usuário' %></title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container">
      <header>
        <h1>➕ Novo Usuário</h1>
        <nav>
          <a href="/users" class="btn btn-secondary">← Voltar para Lista</a>
          <a href="/tasks" class="btn btn-secondary">📋 Tarefas</a>
        </nav>
      </header>

      <main>
        <div class="card">
          <h2>👤 Cadastrar Novo Usuário</h2>

          <form action="/users" method="POST" class="user-form">
            <div class="form-group">
              <label for="name">Nome Completo *</label>
              <input
                type="text"
                id="name"
                name="name"
                class="form-control"
                placeholder="Digite o nome completo"
                required
              />
            </div>

            <div class="form-group">
              <label for="email">Email *</label>
              <input
                type="email"
                id="email"
                name="email"
                class="form-control"
                placeholder="usuario@exemplo.com"
                required
              />
            </div>

            <div class="form-actions">
              <button type="submit" class="btn btn-primary">
                ✅ Cadastrar Usuário
              </button>
              <a href="/users" class="btn btn-secondary"> ❌ Cancelar </a>
            </div>
          </form>
        </div>

        <!-- Informações -->
        <div class="card">
          <h3>ℹ️ Informações</h3>
          <ul class="info-list">
            <li><strong>Nome:</strong> Use o nome completo do usuário</li>
            <li><strong>Email:</strong> Deve ser único no sistema</li>
          </ul>
        </div>
      </main>
    </div>

    <script src="/js/app.js"></script>
  </body>
</html>
